Just have authentication and notification services for now.

Setup hooks:
`git config core.hooksPath .githooks`

To run them, simply run `docker-compose --profile all up -d` and test out the api
